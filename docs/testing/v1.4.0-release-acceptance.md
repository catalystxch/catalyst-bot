# CATalyst v1.4.0 release acceptance

All timestamps use Europe/London. A gate is complete only when the recorded
command or live acceptance step has succeeded against the stated commit and
the supporting evidence is attributable to that commit. This document must
not be used to justify a merge or release while any required item is pending.

## Candidate identity

- Branch: `codex/post-tibetswap-v1-4`
- Pull request: <https://github.com/catalystxch/catalyst-bot/pull/214>
- Candidate code commit: `6a8b350d3a5a7b104430e8cb6d11a52a3c98db73`
- Target release: `v1.4.0`
- GitHub `main` merge SHA: Pending
- Release tag: Pending

## Scope

- Replace retired TibetSwap runtime dependencies with attributable Dexie,
  Splash, Sage, Spacescan, and Coinset offer-book and chain evidence.
- Add durable market-confidence, manipulation-resistance, progressive
  withdrawal, recovery, confirmed-fill accounting, profitability, and
  competition gates.
- Preserve historical TibetSwap data as read-only migration history while
  marking its live capabilities retired.
- Integrate only the reviewed, compatible portions of PRs #213, #208, and
  #207. Those pull requests remain open until final acceptance and release.

## Source verification

| Gate | Result |
| --- | --- |
| Full serial pytest | 6,174 passed, 71 skipped, 422 subtests passed in 877.69s |
| Post-Tibet lifecycle/fault matrix | 326 passed in 65.18s |
| Opt-in browser E2E | 56 passed in 48.19s |
| Ruff check and format | Passed |
| Vulture | Passed |
| Bandit | Zero medium- or high-severity findings |
| `.env.example` parity | All 102 keys passed |
| Pull-request CI | All nine checks passed on pushed documentation head `d01a2a7`; refresh pending for the final review follow-up |

The Windows package built from the clean detached `31b27c5d0054079174a518f99a646b560d0b51ad`
candidate source passed the
packaged Sage RPC, packaged API, first-launch desktop, and upgrade-publication
recovery smoke tests. The build used the repository's unreleased runtime
version because release metadata is deliberately changed only by the release
tag workflow; it is therefore build evidence, not the final v1.4.0 installer.
The packaged executable SHA-256 was
`8310526041FFF65762E123B660ABC3A97E965CA07237BA10510DAD7FF8D696C4`.
The 195-file candidate archive
`Catalyst-v1.4.0-candidate-31b27c5-windows.zip` was fully decompressed and
read back; its SHA-256 was
`B312438DC7DA3C2C6BDD9FEEB9391569001A826398235E696BFEB5F77CEC848`.
Microsoft Defender platform `4.18.26080.3`, engine `1.1.26080.3`, and
definitions `1.459.154.0` then scanned the complete clean packaged
`dist/Catalyst` directory and the candidate archive and reported no threats.
All GitHub checks passed on the exact candidate, the exact-head Codex review
reported no major issue, and the pull request had no unresolved review thread
when this evidence was recorded.

The named post-Tibet lifecycle/fault matrix was repeated on 2026-09-11 against
the current branch using `test_post_tibet_full_lifecycle`, provider contracts
and adapters, market confidence, degraded state, publication outbox/discovery,
fill confidence and authority closure, migration, no-live-Tibet, and Sage bulk
cancel tests. All 326 tests passed in 65.18 seconds. Together they exercise the
mock-wallet Smart Settings/Coin Prep/start/publish/discover/fill/replace/stop/
cancel/restart lifecycle; controlled provider outage, staleness, conflict, and
recovery; manipulated and shallow books; exact Sage fill authority; durable
publication recovery; migration; and the prohibition on every live TibetSwap
network path. This deterministic gate does not replace the real two-PC cycle.

During the PC 1 packaged audit, the dashboard CAT-list Refresh action was
observed clearing the still-valid MZ/XCH selection and its setup readiness.
The failure was reproduced by an opt-in browser regression test, fixed in
`72186dc440cccedf6dca6fcb521e19021a08c473`, and rechecked in the rebuilt
package. MZ/XCH, the exact fingerprint, balances, and RED safety state all
remained intact after the same live Refresh action.

On 2026-09-11, PC 1 also completed a read-only packaged UI audit against the
candidate code: Sage TEST 7 fingerprint `736588221`, wallet ID `2`, mainnet,
`MZ_XCH`, and the exact Monkeyzoo Token asset were verified. Dashboard, Offers
active/history, P&L, Market Intel, Settings live/setup, Logs, Data Reset, Help,
and About all rendered successfully. Logs immediately backfilled the complete
non-debug current session after repeated Dashboard/Logs switches. The Doctor
reported all 10 checks passed. Splash was independently verified as running
with three connected peers; after a fresh Market Intel fetch, the UI matched
the live node PID, uptime, peer count, and empty relevant-offer state. RED
confidence showed no tradable range, all exposure withdrawn, exact external
fill proof unavailable, and Sage as the authoritative fill source. No
financial mutation was attempted.

The same live audit then exposed a contradictory legacy dashboard card: the
durable safety snapshot was RED with withdrawal `ALL`, but Market Health said
"Market conditions healthy — bot stopped." Two browser regressions reproduced
both request-order races (confidence before dashboard and confidence after
dashboard). Commit `4a77dad079426988f4091ef7fcdb0db480e9c119`
now renders the stricter authoritative confidence state immediately. A clean
package rebuilt from that exact commit passed all packaged smokes and was
restarted against Sage TEST 7. With the exact MZ/XCH pair selected, the live
card displayed "Market blocked — offer-book confidence is RED" and the
underlying reason codes while creation and requoting remained disabled. No
startup warning, error, traceback, or `BAD_AGGREGATE_SIGNATURE` was observed.

The PC 1 provider-degradation audit then exposed a separate display defect.
When the verified Splash child PID `30488` was terminated, CATalyst detected
the outage, kept offer creation and requoting disabled under RED confidence,
and its supervisor launched replacement PID `64776`; however, the open Market
Intel panel initially retained the dead PID until a reload. A focused browser
regression reproduced this stale-process transition before the fix. Commit
`5b17fe0efdefbd7e7f6298022efcec177af88fc8` adds a view-gated five-second poll
of the lightweight Splash-node status endpoint. All 62 browser tests, 89
related Splash/market tests, and the complete serial suite passed afterward.
The clean packaged build was then launched against the exact required TEST 7
identity and the controlled termination was repeated. Without a reload or tab
switch, Market Intel changed from PID `30488` to PID `64776`, reset uptime,
and matched the authenticated API. The chronological Logs view showed the
expected `splash_node_crashed` warning followed by launch and running events;
the dashboard remained visibly RED with withdrawal `ALL`. This was a
controlled outage test, not an uncontrolled runtime warning, and no financial
mutation was attempted.

Dexie's current settled-trade feed was also sampled live. Its newest MZ trade
was stale, and the candidate retained it as historical evidence without using
it to confirm a current market movement. Spacescan and Coinset remain generic
chain corroboration only; their current public APIs cannot prove exact offer
spend plus created-coin flow, so Sage remains the sole exact fill authority.

The final PC 1 packaged diagnostics audit found that a Sage confirmation line
which omitted the word `fingerprint` could disclose the configured nine-digit
wallet fingerprint in a downloaded debug bundle. A focused regression first
proved the leak. Commit `3cf39928a509d14a66d2a60240f6327e69b4277a`
adds the configured `SAGE_FINGERPRINT` value to the bundle's exact-value
redaction set. The focused regression, all 21 logs-endpoint tests, Ruff, and
the complete serial suite passed. A clean package from the exact commit passed
the packaged Sage RPC, API, desktop-launch, duplicate-owner, persisted-state,
native-safety, and upgrade-publication-recovery smokes. It was then launched
against Sage TEST 7 and the exact MZ/XCH asset while market confidence remained
RED and all financial mutation stayed blocked. A fresh 29-entry debug bundle
(`F7D2EA50DA7276390B33C19250748266C7B0EDEA1CDCFD6D953A202178BE46EF`)
contained no copy of the configured fingerprint and no obvious secret
assignment.

The next PC 1 live cycle used the authorised DBX asset to exercise the
healthy-book path that MZ could not provide. Balanced Smart Settings completed
with 10% XCH and DBX reserves, two-sided operation, the existing fee policy,
and a 45-buy/44-sell limit. Existing prepared inventory was classified ready.
After the bot started, the publication scheduler staged no more than ten
offers per cycle and waited for exact Dexie rediscovery of each stage. The
complete 89-offer ladder was then visible in CATalyst, Sage, and Dexie as
45 buys and 44 sells, with zero runtime errors. The full ladder remained stable
for at least 28 complete cycles with no fill, signing failure, traceback, or
`BAD_AGGREGATE_SIGNATURE`. The bot then stopped normally. Sage native bulk
Cancel All reached authoritative terminal proof for all 89 offers (zero
failures and zero pending), and CATalyst, Sage, and the local database all
returned to zero offers. The terminal reconciliation took approximately four
and a half minutes; its long-duration `retry_failed_cancels` trace was followed
by `wallet_sync_expected_empty` and `cancel_all_confirmed`, so it was not a
failed cancellation.

That extended run exposed one CATalyst display defect: each offer's Dexie badge
correctly said it was live while the adjacent publication and discovery detail
incorrectly said "not queued" and "not observed". The database and superlog
both held exact Dexie publication and rediscovery evidence. A focused status
endpoint regression failed first and proved that `/api/status`, which feeds the
Offers tab, omitted the durable authority attached by `/api/offers`. The
smallest fix now attaches the same authority to the status payload. A second
regression protects the new bounded batch database read. On the live 89-offer
set this reduced authority enrichment from 484.39 ms of per-offer reads to
13.46 ms while retaining all 89 exact Dexie observations. The focused tests,
204 related regressions, four related subtests, Ruff, and diff validation pass.
The authority-fix full suite passed 5,880 tests, skipped 63, and passed 422
subtests in 813.40 seconds. Exact-head package gates remained required after
that clean build.

The authority fix was then packaged cleanly at documentation head
`9ad6b9c51bdde9db6c9a9bdb4803dc21a481ad38`, whose candidate code parent is
`a35f4680ece67dc5dfec14d9cde9ed48cac49c97`. Packaged Sage RPC,
authenticated API, clean/duplicate/persisted/native-safety desktop launch, and
upgrade-publication recovery smokes all passed. The unsigned executable is
10,927,780 bytes with SHA-256
`C2606BE4DB8BC39E80CA00CE303EC5CD34458C8BE8032C602DF7D6421AC213F4`.
Microsoft Defender engine `1.1.26080.3` with definitions `1.459.160.0`
reported no threat for the complete packaged directory.

That exact package was launched against Sage TEST 7 on mainnet and the exact
DBX asset. Balanced Smart Settings again completed with 10% XCH and DBX
reserves, two-sided operation, 45 buys, 44 sells, fee amount
`0.0000130791` XCH, and 50 dedicated `0.001` XCH fee coins. Existing prepared
inventory was correctly reused. Startup self-test passed and the staged ladder
reached all 89 offers, then completed another full-ladder cycle with zero
errors. In the corrected Offers view, provider details progressed from
`Dexie succeeded · Splash queued` and `Dexie pending · Splash pending` to
`Dexie exact · Splash pending` as exact rediscovery arrived. No offer rendered
the former false `not queued` or `not observed` claims.

After a second authoritative 89/89 DBX bulk cancellation returned the wallet,
database, and UI to zero offers, PC 1 selected the exact SBX asset and set 10%
XCH and SBX reserves. The fresh SBX Smart Settings request observed 100 bids
and 76 asks but a severely dislocated book: best bid `0.00000128` XCH and best
ask `0.00038910` XCH, approximately a 198.7% spread. The backend correctly
returned `Smart Settings requires GREEN offer-book confidence`; no settings,
Coin Prep, offer, or wallet mutation followed.

That live SBX failure exposed a separate GUI ordering defect. The incentivized
pair prompt asked whether to maximize DBX rewards before the Smart Settings
request had proved that a usable plan existed. A focused UI-contract regression
first failed with the error guard at character 8,426 and the rewards prompt at
2,148. Candidate code commit
`b3def4b51c571c181ae2d758d54622d949b9c619` now waits for a successful
Smart Settings response before offering incentive optimization; choosing the
cap on a valid plan performs one explicit capped recalculation. The regression,
81 related settings/UI tests, JavaScript syntax check, and all 62 opt-in browser
tests pass. A live source recheck against the same TEST 7/SBX identity and 10%
reserves displayed only the real GREEN-confidence error, with no rewards modal
and no mutation. The fresh full suite for this newest candidate passed 5,881
tests, skipped 63, and passed 422 subtests in 785.70 seconds. An exact-head
package was then built from candidate code commit
`b3def4b51c571c181ae2d758d54622d949b9c619`. Packaged Sage RPC,
authenticated API, clean/duplicate/persisted/native-safety desktop launch, and
upgrade-publication recovery smokes all passed. The unsigned executable is
10,927,780 bytes with SHA-256
`CA078BB38F99BF8EB9EED24AFFFA1361C4116561400E2F3AB9B4DF892E3D7362`.
Microsoft Defender engine `1.1.26080.3` with definitions `1.459.160.0`
reported no threat for the complete packaged directory.

That exact package was launched through the real GUI, connected to Sage TEST 7
fingerprint `736588221` on mainnet, and selected the exact SBX asset
`a628c1c2c6fcb74d53746157e438e108eab5c0bb3e5c80ff9b1910b3e4832913`
with wallet ID 2. Both reserves were visibly set to 10% (`14.592` XCH and
`46,142` SBX) and Balanced Smart Settings was invoked. The package displayed
only `Smart Settings requires GREEN offer-book confidence`; it did not show
the unrelated DBX-rewards prompt, start Coin Prep, save a plan, or mutate the
wallet. Logs ended at `smart_defaults_market_confidence_blocked`, the Offers
view remained at zero buy and zero sell offers, and the package then shut down
cleanly with no open offers left behind.

That cycle exercised the staged-publication and direct-prep candidate which
was committed as `4425aa01054b3d100b9a43f7d7a01f80498d821e`. Because the
live package was built before the mechanically formatted source was committed,
it is strong functional evidence but is not represented as a cryptographic
package attestation for that commit. A clean rebuilt artifact and its smoke
checks remain required before merge or release.

That exact runtime source was subsequently rebuilt cleanly at documentation
head `32a7c8238436879a0a457d67da4a30c4d960ad94`. Packaged Sage RPC,
authenticated API, clean/duplicate/persisted/native-safety desktop launch, and
upgrade-publication recovery smokes all passed. The resulting unsigned
`Catalyst.exe` is 10,926,467 bytes with SHA-256
`400870BF6CC8B84C600B811CF24B8C5DC60FCA868ED5C15C966F42CFF4CA9AD2`.
Microsoft Defender engine `1.1.26080.3` with definitions `1.459.160.0`
reported no new threat for the complete packaged directory. This exact-head
package proof supersedes the pre-commit packaging caveat above for automated
smokes; the DBX financial cycle remains separately attributable functional
evidence.

SBX was then selected from the authoritative zero-offer state and checked
against its exact configured asset ID. A fresh Smart Settings request sampled
85 asks and 100 bids from Dexie but returned RED confidence with
`out_of_range_depth_excluded` and `insufficient_bid_depth`. CATalyst therefore
refused to save settings or start Coin Prep. No offer or wallet mutation was
made. This was a correct fail-closed response to the evidence CATalyst had
selected, but the later root-cause investigation below proves that the selected
buy-side page was wrong and therefore supersedes the earlier classification of
the market itself as RED.

### 2026-09-12 SBX reciprocal-sort defect and live source correction

A cross-market review of all 541 CAT/XCH tickers exposed a reproducible
pagination defect in both standalone Smart Settings and running Market Intel.
Dexie reports buy-direction prices as CAT per XCH. CATalyst requested
`sort=price_desc` and then converted each row to XCH per CAT, so the requested
page contained the worst rather than best bids. DBX had only 53 active bids and
therefore happened to return its complete book. SBX had 964 active bids while
Dexie capped the response at 100; CATalyst consequently saw only the worst 100
and reported a false best bid near `0.00000128` XCH.

The same live endpoint with the correct reciprocal `sort=price_asc` returned an
SBX best bid of `0.000377082959130869...` XCH against best ask `0.000389096`
XCH, approximately a 3.1% spread. A production-path read-only probe after the
fix saw 100 best-page bids, 79 asks, and derived GREEN Balanced confidence for
a 1 XCH configured offer, with 200 XCH bid depth and 209.46973 XCH ask depth
inside the confidence envelope. No wallet or offer mutation was performed.

Focused boundary regressions first failed twice on the emitted `price_desc`
parameter, then passed after changing only the two buy-side queries to
`price_asc`. The complete Smart Settings, Market Intel, market-runtime, and
market-confidence slice passed 164 tests, and Ruff plus `git diff --check`
passed. This correction retains every existing confidence and fail-closed
threshold; it fixes the evidence selection rather than weakening policy.

### 2026-09-12 zero-offer startup fresh-reset conflict

The live browser showed repeated HTTP 409 responses from
`POST /api/session/fresh-start` during ordinary page loading. The read-only
`/api/check-resume` response correctly proved that Sage had zero live offers,
but the no-resume frontend branch then requested a full authoritative reset
without any user action. Durable fill history caused the database safety guard
to reject that reset with `authoritative_state_conflict`; the guard was correct,
but the unsolicited reset request and visible conflict were a frontend defect.

A focused regression first failed against the no-resume branch. The branch now
updates only local presentation state and reserves the guarded reset endpoint
for the explicit Start Fresh control. The frontend, startup-flow, shutdown/
resume, and session-endpoint regression slice passed 100 tests. A live browser
reload then emitted `GET /api/check-resume` with HTTP 200, no fresh-start POST,
and zero console errors or warnings. No wallet, offer, or database mutation was
performed during the live verification.

### 2026-09-14 sell-ladder Coin Prep/runtime sizing correction

An exact packaged SBX cycle exposed a shared-sizing defect after successful
Coin Prep. Coin Prep correctly derived CAT output amounts from the generated
sell-ladder prices (`5326.59`, `4241.99`, `3068.21`, and `1588.45` SBX), but
runtime top-up independently divided the configured XCH tier sizes by the
single mid price. It therefore expected different outer and extreme amounts,
classified 28 valid prepared coins as misfits, absorbed them into reserve, and
left the live book unable to expand safely beyond its initial inner tier.

A focused regression first failed by comparing runtime CAT target sizes with
the shared sell-ladder-aware Coin Prep calculation. Runtime top-up now consumes
the same canonical `get_tier_sizes_mojos_from_cfg(is_cat=True)` result used by
Coin Prep, the post-prep drift gate, classification, and selection. The focused
Coin Prep/top-up slice passed 124 tests after mechanical formatting. The exact
formatted tree then passed 6,173 tests with 71 skipped and 422 subtests, Ruff
check and format, Vulture, Bandit with zero medium/high findings, and all 102
`.env.example` parity checks. Opt-in browser E2E passed 56 tests.

The live repair cycle used Sage 0.13.0, TEST 7 fingerprint `736588221`,
mainnet, wallet ID 2, and exact SBX asset
`a628c1c2c6fcb74d53746157e438e108eab5c0bb3e5c80ff9b1910b3e4832913`.
Balanced two-sided settings remained unchanged at 45/45 configured offers,
15% XCH reserve (`21.883` XCH), 10% SBX reserve (`46142` SBX), 50 fee coins of
`0.001` XCH, and manual fee `0.0000130791` XCH. Repair Coin Prep completed in
two authoritative batches with the four exact CAT tier sizes above and no
drift. The bot reached the correct AMBER provider-confidence cap of 11 buy and
11 sell offers, completed repeated clean cycles with zero application errors,
and preserved all outer/extreme prepared CAT inventory instead of absorbing it.
Splash continued to report external `InsufficientPeers`; CATalyst visibly
marked Splash degraded while Dexie remained valid and authoritative.

The exact live 22-offer book reconciled as 22 CATalyst database rows, 22 Sage
fillable offers, and 22 Dexie status `0` offers. Reload restored the running
session, exact wallet/pair/configuration, 11/11 book, balances, and AMBER cap.
Every main UI tab activated, and Logs immediately backfilled and advanced in
chronological order through a later completed cycle. The bot was then stopped
and Sage native bulk Cancel All completed all eight batches: 22 confirmed,
zero pending, and zero failed. Final reconciliation showed zero CATalyst open
offers, zero Sage fillable offers, all 22 Dexie rows at status `3`, and an
allowed safety state with no blocker.

The source-identical Windows package built from code commit
`11ecada56c2797ab5b3e46dd4294a98e71aef175` passed packaged Sage RPC,
authenticated API, clean/duplicate/persisted/native-safety desktop launch, and
upgrade-publication recovery smokes. Its `Catalyst.exe` is 11,097,526 bytes
with SHA-256
`D67CA1D530B8ABA8920CF7D4D70EDB585244BCD2C6FA50EF82FD1A3C315CF0EA`.
Microsoft Defender reported no threat for the complete packaged directory.

### 2026-09-14 final review follow-up

The final PR hygiene pass found two unresolved automated review comments in the
Market Bootstrap Coin Prep path. A focused assertion first proved that the
campaign's authoritative XCH and CAT balances were assigned and then reset to
zero before sufficiency was calculated. The verifier now preserves the exact
campaign balances while still fetching spendable coin records independently.
A second regression proved that the trigger returned and logged arbitrary
internal `ValueError` detail. Bootstrap failures now map to stable public reason
codes, so filesystem paths or other internal exception text cannot reach the
client or normal event log.

The two focused red/green regressions and an adjacent boundary test passed,
followed by 67 Bootstrap/Coin Prep regressions, Ruff check and format, Vulture,
the repository-configured Bandit
policy, and the complete serial suite: 6,174 passed, 71 skipped, and 422
subtests passed in 877.69 seconds. The source-identical package built from code
commit `6a8b350d3a5a7b104430e8cb6d11a52a3c98db73` passed packaged Sage RPC,
authenticated API, clean/duplicate/persisted/native-safety desktop launch, and
upgrade-publication recovery smokes. `Catalyst.exe` is 11,097,793 bytes with
SHA-256
`E3ACC9FECEA3486F63770784E7E1D04A140DFA7B81C49AA93597E45E0CCEA219`.
Microsoft Defender engine `1.1.26080.3` with definitions `1.459.201.0` reported
no threat for the complete packaged directory.

## Release-workflow readiness

The tagged `Build Release` runs for v1.3.17 through v1.3.21 all ended with
GitHub conclusion `startup_failure` and created zero jobs, while v1.3.16 had
completed successfully. The workflow files at both boundary tags and the
current candidate pass actionlint 1.7.12. The repository Actions policy was
then inspected and proved the external cause: it allowed only selected actions,
disabled all verified-publisher actions, and did not allow the exact pinned
SignPath action introduced after v1.3.16. On 2026-09-11 the repository policy
was narrowed to add only
`signpath/github-action-submit-signing-request@c92b958760219087e01f8d67a1669ed57afe2627`;
GitHub's API readback confirmed that exact allow-list entry and retained
`verified_allowed: false`.

This configuration correction permits GitHub to construct the signed workflow
instead of rejecting it before job creation. It does not enable SignPath
credentials or weaken the signed workflow: because the Foundation application
was declined, its named configuration preflight must still fail closed. The
approved no-cost Windows route remains the separate, explicitly labelled
unsigned-beta process in `docs/CODE_SIGNING_POLICY.md`; it must pass installer
identity, SHA-256, NotSigned, Defender, install, and packaged-runtime checks
before publication. A final release tag has not been created during acceptance.

At 13:59 Europe/London on 2026-09-11, after all nine GitHub checks passed on PR
head `444f4810465ca6c65157f4952d7ca1087faaf0ba`, PC 1 independently repeated
the stopped-runtime Smart Settings safety gate in the packaged candidate. Sage
TEST 7 fingerprint `736588221`, mainnet, wallet ID `2`, `MZ_XCH`, and the exact
Monkeyzoo asset were selected first. The form held the approved 10% XCH and MZ
reserves, two-sided mode, explicit `0.0000130791` XCH transaction fee, 50 fee
coins of `0.001` XCH, and its existing risk settings. With current Dexie depth
still producing RED confidence and withdrawal `ALL`, Smart Settings returned
`Smart Settings requires GREEN offer-book confidence`. Every inspected form
value and the Coin Prep preview remained unchanged; no setting was saved and no
wallet mutation occurred. This proves fail-closed behavior under the present
external market-quality limitation, not the healthy-market sizing acceptance
that remains pending.

## Required live identity

Every financial action on either PC must first prove all of these values:

- Network: `mainnet`
- Wallet type: Sage
- Wallet: `TEST 7`
- Fingerprint: `736588221`
- Virtual CAT wallet ID: `2`
- The selected ticker and asset ID must be one of these explicitly authorised
  exact pairs:
  - `MZ_XCH` — Monkeyzoo Token:
    `b8edcc6a7cf3738a3806fdbadb1bbcfc2540ec37f6732ab3a6a4bbcd2dbec105`
  - `DBX_XCH` — DBX:
    `db1a9020d48d9d4ad22631b66ab4b9ebd3637ef7758ad38881348c5d24c38f20`
  - `SBX_XCH` — SBX:
    `a628c1c2c6fcb74d53746157e438e108eab5c0bb3e5c80ff9b1910b3e4832913`

If any value differs, the live gate stops without creating, cancelling, or
preparing coins.

## Two-PC functional acceptance

Both PCs must independently complete the following against the same candidate
commit or an installer artifact cryptographically tied to it:

- [ ] Install/launch and prove the exact candidate identity.
- [ ] Verify all UI tabs, current-session Logs backfill, and live updates.
- [ ] Run Smart Settings with the approved reserve scenario.
- [ ] Complete variable-size XCH and MZ Coin Prep with explicit fees.
- [ ] Start and stop the bot without an authoritative-state conflict.
- [ ] Exercise offer creation, publication, fill, and requote handling.
- [ ] Exercise Sage native bulk Cancel All and verify authoritative completion.
- [ ] Restart and verify existing-ladder Continue/Start Over recovery.
- [ ] Confirm CATalyst, Sage, Dexie, and Spacescan agree on attributable state.

### Per-PC live status

| Live gate | PC 1 | PC 2 |
| --- | --- | --- |
| Exact candidate install/launch and identity | Passed from commit `d80cd9c721204f7b627cd084e4ec395f66e58c80`; packaged `Catalyst.exe` SHA-256 `2c9ebf8d9a1ebe8651cc375e2d263145b14c60e306bcc028142415bb907eb847` | Passed on the locally built candidate using Sage 0.13.0, Chia mainnet, Harvestr fingerprint `3702373391`, wallet ID 2, and the exact MZ asset; the observed live package SHA-256 was `C38977EAB9D0E36BDC557CBB34F0C9C720C73ABAC73525EA9275040749F73A1F` |
| Every UI tab, Logs backfill, and live updates | Passed on the exact packaged candidate across Dashboard, Offers, P&L, Market Intel, Settings Live/Setup, Logs, Data Reset, Help, and About; Logs backfilled the current session and continued in chronological order | No UI defect reported during the 226-cycle run; complete tab-by-tab and reload acceptance remains pending |
| Smart Settings with approved reserves | Passed live with DBX at 10% reserves; MZ rejection passed without mutation; SBX best-book selection and adaptive confidence capacity are active at an AMBER target of 11 buy / 11 sell while Splash has no peers | Passed the safe MZ setup path; current RED confidence correctly retained zero exposure rather than manufacturing a trusted price |
| Changed-size XCH/CAT Coin Prep with explicit fees | Passed on SBX after the canonical sell-ladder sizing correction: two authoritative batches, exact tier inventory, no drift, and the configured `0.0000130791` XCH fee | Coin Prep passed; the changed-size second cycle remains pending |
| Bot start/stop | Passed live with DBX and from the exact packaged SBX candidate; the post-fix SBX bot reached and held the correct 11/11 AMBER cap before a deliberate clean stop | Started and remained healthy for at least 226 cycles; deliberate stop/restart acceptance remains pending |
| Create/publish/fill/requote | DBX creation and exact staged publication passed; fill and requote remain pending | Blocked safely with zero offers by legitimate RED MZ confidence; not passed |
| Native Sage bulk Cancel All | Passed live for all 89 DBX offers and again for the post-fix 22-offer SBX book; the SBX run completed 8/8 batches with 22 confirmed, zero pending, and zero failed | No active MZ ladder existed to exercise Cancel All under load; pending |
| Existing-ladder restart recovery | Passed twice with the live 22-offer SBX ladder, including the exact final packaged candidate: Continue/Start Fresh was shown, 11 buy / 11 sell were restored, and Resume Bot completed | No active MZ ladder existed to exercise recovery; pending |
| Sage/Dexie/Splash/chain agreement | Passed live for the full 45/44 DBX ladder and the final zero-offer state; exact packaged SBX recovery matched Sage, CATalyst DB, Dexie, and verified visibility at 11 buy / 11 sell with zero sync or order-book errors | Passed for the observed zero-offer state: no stale active offers, duplicate spends, locked coins, pending cancellations, runtime findings, or order-book errors; Splash was degraded/empty |

RED safety remains an external market-quality limitation for the present MZ/XCH
book, not permission to mark a live trading gate passed. DBX/XCH provided the
fresh GREEN-confidence path used for the completed live gates above. SBX/XCH's
corrected exact book has sufficient two-sided depth, while current policy
demotes it to AMBER when Splash is enabled but has no peers. The implemented
adaptive confidence capacity therefore limits the live SBX book to 11 offers
per side instead of the configured maximum 45 per side. The exact packaged
candidate sustained that 11/11 book for 24 clean cycles before a deliberate
stop/rebuild, then recovered it without creating duplicate exposure. No
remaining row requiring a financial mutation may advance unless fresh
attributable evidence reaches every configured confidence and safety threshold.

## Provider degradation and conflict acceptance

Both PCs must demonstrate safe, visible behavior for controlled Dexie, Splash,
Spacescan, and Coinset outages, stale observations, disagreement, and recovery.
The bot must not substitute stale data, invent confidence, or continue unsafe
two-sided exposure. TibetSwap must remain explicitly retired and must receive
no live request.

## Sustained run

- PC 1 clean-window start/end: 2026-09-14 07:17-07:36 Europe/London (24 clean
  SBX cycles), followed by an exact-candidate restart at 07:41 and a post-fix
  exact-package run at 14:51-15:18; the 24-hour gate is still in progress
- PC 2 clean-window start/end: at least 226 healthy monitoring cycles on the
  locally built MZ candidate; the full 24-hour duration and live-ladder load
  remain pending
- Required duration: 24 continuous hours on each PC
- Unresolved confirmed defects: 0 at the latest PC 1 checkpoint

Any confirmed CATalyst defect resets the affected acceptance gate after a
focused failing regression, minimal fix, and fresh verification. External
provider outages are recorded separately and are not labelled CATalyst bugs.

## Final release gates

### Secondary-PC MZ findings integrated on 2026-09-15

PC 2 reproduced two candidate defects from base commit
`3bd9260822de1fa080283a80ddda57bda82225de` while testing MZ with Sage
0.13.0 on Chia mainnet. PR #215 found that the Smart Settings preview and
`/api/coin-prep/verify` did not use the worker's complete per-side economic
plan. The original 45/45 plan required 237.4993 XCH against 216.7406 XCH
available after reserve and was stopped before broadcast. After the fix, the
AMBER-capped 36/36 MZ plan completed Coin Prep in approximately 2 minutes 14
seconds, with two confirmed batches, zero missing tiers, and successful
prepared-inventory recovery after restart. PR #216 corrected the Settings fee
hint so the configured `0.0000130791` XCH is displayed exactly instead of the
misleading rounded value `0.00001308`.

The primary PC reviewed and integrated both commits into PR #214 at
`48332134cbc38834a2ba0c4bbe0a946a489c2f3a`. The combined tree passed 6,182
unit tests, 81 expected skips, 422 subtests, all 80 browser E2E tests, the 42
focused Coin Prep endpoint tests, Ruff lint and formatting, environment parity,
Vulture, a clean Windows build, and all four packaged Sage/API/desktop/upgrade
smokes. Microsoft Defender reported no detection for the rebuilt package. The
unsigned 11,098,738-byte `Catalyst.exe` has SHA-256
`2C5CE3DD2A8A27F254065BF6844E24C234CDC1C4F10C9610F79739D32118259E`.

This integration does not yet claim the remaining PC 2 live-ladder, fill,
requote, bulk-cancel, changed-size repeat, or sustained clean-window gates;
those must continue from the exact combined commit above.

### Secondary-PC RED-market and warning-latch follow-up on 2026-09-15

PC 2 continued on MZ/XCH with the exact asset ID and reported successful Coin
Prep followed by at least 226 healthy bot cycles. Dexie exposed 5 buys and 32
sells, but only 0.6375 XCH of eligible ask depth against 8.611 XCH required.
Together with degraded/empty Splash data, confidence correctly remained RED for
`out_of_range_depth_excluded`, `insufficient_ask_depth`, and
`single_provider_dependency`. CATalyst therefore retained zero live offers.
This is the intended fail-closed response to an unsafe market, not a passed
live-ladder exercise and not a CATalyst defect.

PR #217 fixed a separate presentation defect in that degraded state: the same
RED/no-trusted-price condition repeatedly emitted an identical warning. The
primary PC added a recovery regression proving that a warning is suppressed
only while the condition persists and is re-armed after trusted pricing
recovers. PR #217 was squash-merged into `codex/post-tibetswap-v1-4` as
`43356e2093776584b84c38a865ef9e9fb02c7db8`. Its revised exact head passed
6,176 tests, 71 expected skips, 422 subtests, 70 browser E2E tests, Ruff lint
and formatting, and a clean Windows build. The secondary PC's prepared package
for that review had SHA-256
`8FC484D73EE5F5FA298BF5EA58C1BB725997B5FC61E231DE9AF6AA83CB3CB5AF`.

A primary-PC regression now also proves that a technically valid two-sided
book with a spread wider than 1,000 bps is rejected by normal Market Follow
before wallet access and returns `BOOTSTRAP_SUGGESTED`. Wide and immature CAT
markets remain usable only through an explicitly authorized, budgeted
Bootstrap campaign; normal Smart Settings cannot silently expose the wallet to
the midpoint of an extreme spread. Existing large-move tests separately prove
that uncorroborated jumps do not re-anchor trusted price, material changes need
persistence, and the hard move cap stays fail-closed.

- [x] Complete PR review and all required GitHub checks are green.
- [ ] Complete both-PC functional and degradation acceptance.
- [ ] Complete both 24-hour clean windows with no unresolved defect.
- [ ] Merge PR #214 into `main`.
- [ ] Tag `v1.4.0` and complete the approved unsigned-beta release workflow.
- [ ] Verify installer version, hash, installation, startup, and update path.
- [ ] Update the live website and download the public asset on clean Windows.
- [ ] Close or supersede PRs #213, #208, and #207 with integration links.
